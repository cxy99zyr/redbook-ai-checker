{
  "name": "redbook-ai-checker",
  "version": "1.0.0",
  "build": {
    "command": "npm install && npm run build",
    "outputDirectory": "dist"
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
